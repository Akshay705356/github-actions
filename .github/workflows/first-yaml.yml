name: first-workflow-practice
on: workflow_dispatch
jobs:
  my_job:
    name: testing first job
    runs-on: ubuntu-latest
    steps:
      - name: first command
        run: echo "hello world"
      - name: second command
        run: |
          echo "here i am using multiple line"
          echo " mutiple commands are being used here"
    
